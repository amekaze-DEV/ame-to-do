# AME to do 项目推进指南（TRAE 开发版）

> 本指南面向不具备专业编程背景、但希望借助 TRAE 独立完成 AME to do 项目的个人开发者。全程以 TRAE 读文档、写代码、改文件、运行命令和调试问题，人类负责确认需求、验证结果和做产品判断为基本原则。

---

## 一、开发模式说明

### 1.1 角色分工

| 角色 | 职责 | 工作占比 |
|---|---|---|
| **TRAE** | 读取项目文档、编写代码、修改文件、运行命令、调试报错、解释技术方案、生成测试数据 | 约 80% |
| **你（人类）** | 提出需求、确认方案、验证功能、管理项目进度、处理 TRAE 无法代替你完成的外部事务（如注册账号、配置服务器、真实重启电脑测试） | 约 20% |

### 1.2 工作流

```text
你提出需求/任务 ──► TRAE 读取文档并实施 ──► TRAE 运行可自动验证的命令 ──► 你验证真实体验 ──► 反馈问题给 TRAE ──► TRAE 修复
         ▲                                                              │
         └──────────────────────── 确认通过 ←────────────────────────────┘
```

### 1.3 关键原则

1. **不要自己写代码**，把写代码、改文件、查错和运行基础验证的工作交给 TRAE。你的价值在于判断功能是否正确、体验是否良好。
2. **逐任务推进**，严格按照 `todo.md` 的任务顺序，完成一个再开始下一个。
3. **及时验证**，TRAE 每完成一个任务，你立即打开应用或查看结果，不要累积多个任务后再统一验证。
4. **让 TRAE 先读文档再动手**，每个任务开始时要求 TRAE 先阅读 `requirements.md`、`design.md`、`todo.md` 中的对应章节，再修改代码。
5. **所有决策写回文档**，需求变更先让 TRAE 更新 `requirements.md` 和 `design.md`，再进入代码修改。

### 1.4 使用 TRAE 的基本方式

TRAE 可以直接读取和修改你选择的项目文件夹，也可以运行安装、构建、测试等命令。你不需要把整份文档复制给 TRAE，只需告诉它参考哪些文件和任务编号。

| 场景 | 推荐让 TRAE 做什么 | 你需要做什么 |
|---|---|---|
| 开始新任务 | 读取 `todo.md` 对应任务，结合 `requirements.md`、`design.md`、`progress.md`、`risk.md` 实施 | 确认任务编号是否正确 |
| 修改需求 | 先更新 `requirements.md`，再同步 `design.md` 和 `todo.md` | 描述你想改什么 |
| 写代码 | 让 TRAE 修改实际项目文件，并运行构建或测试 | 查看应用是否符合预期 |
| 调试报错 | 让 TRAE 读取错误日志、相关文件并修复 | 提供你看到的异常现象 |
| 生成发布包 | 让 TRAE 执行构建命令并检查输出文件 | 在真实 Windows 环境中双击运行测试 |

TRAE 适合处理代码、文档、终端命令和本地文件。注册账号、输入第三方密码、重启电脑、在另一台电脑上测试、确认产品体验，这些仍然需要你完成。

---

## 二、前置准备

### 2.1 必须准备的硬件与环境

| 项目 | 要求 | 说明 |
|---|---|---|
| 电脑 | Windows 10/11 64位 | 必须，因为初始版本目标平台是 Windows |
| 内存 | 8GB+ | 开发时需要同时运行编辑器、浏览器、应用 |
| 磁盘空间 | 10GB 空闲 | Rust 编译器和构建产物占用较大 |
| 网络 | 稳定宽带 | 下载依赖、TRAE 交互、WebDAV 测试都需要 |

### 2.2 必须安装的软件

按以下顺序安装，每装完一个让 TRAE 验证：

| 软件 | 用途 | 安装方式 |
|---|---|---|
| **Node.js** | 前端构建工具 | 官网下载 LTS 版本安装包，双击安装 |
| **Rust** | Tauri 桌面应用后端 | 官网下载 `rustup-init.exe`，按默认选项安装 |
| **Git** | 代码版本管理 | 官网下载安装包，使用默认配置 |
| **VS Code** | 代码编辑器 | 官网下载安装 |
| **Tauri CLI** | 创建和构建 Tauri 项目 | 安装完 Rust 后，在终端运行 `cargo install tauri-cli` |
| **WebDAV 测试服务** | 测试同步功能 | 可选：坚果云免费账号（推荐）或让 TRAE 帮你搭建本地测试服务器 |

> **TRAE 提示词模板**："请帮我检查当前系统是否已安装 Node.js、Rust 和 Git。如果没有，请给出具体的安装步骤和验证命令。"

### 2.3 必须注册的账号

| 账号 | 用途 | 注册地址 |
|---|---|---|
| **坚果云** | WebDAV 同步测试 | jianguoyun.com，注册免费版即可 |
| **GitHub** | 代码托管（可选但强烈推荐） | github.com |

---

## 三、按阶段推进指南

### Phase 1：项目初始化（预计 1-2 天）

**你的主要工作**：

1. **创建项目**：让 TRAE 读取 `todo.md` 中的 Task-01：
   > "请帮我执行 Task-01：使用 `npm create tauri-app@latest` 创建 AME to do 项目，选择 React + TypeScript 模板。创建完成后验证 `npm run tauri dev` 能正常启动。"

2. **逐个验证**：每完成一个任务，你在终端运行指定命令验证是否成功。

3. **遇到问题**：把报错信息完整交给 TRAE，让它读取相关文件并修复。不要自己盲目搜索修改。

**常见问题**：

| 问题 | 处理方式 |
|---|---|
| Rust 安装失败 | 让 TRAE 检查环境变量 PATH 是否包含 Rust |
| `tauri dev` 启动白屏 | 让 TRAE 检查端口是否被占用 |
| 构建报错 | 把完整报错日志交给 TRAE |

---

### Phase 2：核心框架开发（预计 3-4 天）

**你的主要工作**：

1. **提供设计文档**：每个任务开始前，让 TRAE 读取 `design.md` 对应章节。

2. **确认接口设计**：TRAE 写完接口定义后，你不需要看懂代码，只需确认：
   - 这个接口是否覆盖了需求中提到的所有功能？
   - 是否有遗漏？（对照 `requirements.md` 检查）

3. **运行单元测试**：TRAE 写完代码后，可以直接运行测试命令；如果需要你确认权限，它会提示你。

**TRAE 提示词模板**：

```
请帮我执行 todo.md 中的 Task-05：实现事件总线 EventBus。
参考 design.md 中"事件总线设计"章节的具体要求。
要求：
1. 实现 on / emit / off 三个核心方法
2. handler 异常不能中断其他 handler
3. 编写单元测试
4. 所有公共 API 添加 JSDoc 注释
```

---

### Phase 3：平台适配开发（预计 3-4 天）

**你的主要工作**：

1. **权限处理**：部分任务（如开机启动、通知）需要管理员权限。TRAE 会提示你如何操作（如"右键以管理员身份运行 VS Code"）。

2. **真机验证**：这是关键阶段，你必须在真实 Windows 环境中测试：
   - 通知是否真的弹出？
   - 托盘图标是否出现？
   - 开机启动是否生效？（重启电脑测试）

3. **WebDAV 连接测试**：用你注册的坚果云账号测试：
   - 服务器地址：`https://dav.jianguoyun.com/dav/`
   - 用户名：你的坚果云邮箱
   - 密码：在坚果云"第三方应用管理"中生成的应用密码

**TRAE 提示词模板**：

```
请帮我执行 Task-16：实现 Windows 开机启动适配。
我已经以管理员身份运行了终端。
请实现后告诉我如何验证注册表中是否正确添加了启动项。
```

---

### Phase 4：业务模块开发（预计 3-4 天）

**你的主要工作**：

1. **功能验收**：这是最能看出成果的阶段。你作为目标用户（倒班工人/管理层视角）验证：
   - 万年历：能否看到今天的农历？节假日标记是否正确？
   - 事项管理：添加一个待办事项，刷新后是否还在？
   - 提醒功能：设置一个 1 分钟后的提醒，是否真的弹窗？

2. **数据持久化验证**：关闭应用再打开，之前的数据是否还在？

3. **边界情况测试**：
   - 添加一个空标题的事项，系统是否阻止？
   - 设置过去时间的提醒，系统如何处理？

---

### Phase 5：高级功能开发（预计 2-3 天）

**你的主要工作**：

1. **桌面小部件验收**：
   - 小部件是否始终置顶？
   - 拖动位置后重启是否记住？
   - 点击待办是否跳转主应用？

2. **模块开关测试**：
   - 禁用万年历模块后，菜单中是否消失？
   - 重新启用后，之前的数据是否恢复？

---

### Phase 6：集成发布（预计 2 天）

**你的主要工作**：

1. **端到端场景测试**：按照 `requirements.md` 中的六大场景，逐一验证：
   - 场景一：打开万年历，确认今天显示正确的农历和节假日
   - 场景二：添加一个"安全检查"待办，设置高优先级
   - 场景三：设置一个明天上午 8 点的定时提醒
   - 场景四：配置 WebDAV 后点击同步，去坚果云网页版查看是否有文件
   - 场景五：桌面小部件显示今天的待办
   - 场景六：开启开机启动，重启电脑验证

2. **单文件测试**：
   - 把生成的 `AME-to-do.exe` 复制到桌面双击运行
   - 确认桌面出现 `ame-to-do-data` 文件夹
   - 再复制到 D 盘运行，确认重新创建数据文件夹

3. **干净环境测试**：找一台没有安装开发环境的电脑，复制 exe 文件过去测试是否能运行。

---

## 四、与 TRAE 协作规范

### 4.1 每次对话的标准模板

```
【当前任务】
todo.md Task-XX：任务名称

【参考文档】
请先读取 requirements.md、design.md、todo.md 中与本任务相关的章节

【当前状态】
上一个任务已完成 / 当前进行到哪一步 / 遇到了什么报错

【具体需求】
1. 实现 xxx
2. 确保 yyy
3. 添加 zzz 注释

【验证方式】
请先运行可自动验证的命令；完成后我会通过 xxx 方式手动验证
```

TRAE 能直接读取项目文件，所以不要在每次对话里粘贴整份长文档。更好的方式是给出文件名、任务编号和你想达成的结果，让 TRAE 自己读取相关上下文。

### 4.2 推荐任务提示词

```
请执行 todo.md 中的 Task-XX。
开始前请先读取 requirements.md、design.md、todo.md、progress.md 和 risk.md 中对应内容。
实施时请直接修改项目文件，不要只给代码片段。
完成后请运行相关测试或构建命令，并说明修改了哪些文件、如何验证。
如果任务完成，请更新 progress.md；如果遇到阻塞或风险，请更新 risk.md。
```

### 4.3 提供报错的正确方式

**错误做法**："报错了，你修一下"

**正确做法**：
1. 复制完整的报错信息（包括堆栈跟踪）
2. 说明你执行了什么命令
3. 如果错误来自界面操作，说明你点击了什么、预期是什么、实际是什么
4. 让 TRAE 自己读取相关代码文件

```
【报错信息】
（完整复制终端报错）

【执行的命令】
npm run tauri dev

【现象】
应用启动后白屏 / 点击同步按钮无反应 / 通知没有弹出

【请 TRAE 做什么】
请读取相关文件，定位原因，直接修改代码，并运行验证命令。
```

### 4.4 需求变更时的处理

如果你发现功能需要调整：

1. 先让 TRAE 更新 `requirements.md`
2. 再让 TRAE 同步更新 `design.md`
3. 如果任务拆分受到影响，再让 TRAE 更新 `todo.md`
4. 最后告诉 TRAE 基于新设计修改代码

**不要**直接让 TRAE 改代码而不更新文档，否则文档和代码会脱节。

### 4.5 TRAE 工作边界

| TRAE 可以做 | 仍需你完成 |
|---|---|
| 读取和修改项目文件 | 判断产品体验是否符合你的真实使用习惯 |
| 运行安装、构建、测试命令 | 安装需要图形界面确认的软件 |
| 解释报错原因并修复代码 | 注册坚果云、GitHub 等外部账号 |
| 生成数据库结构和迁移脚本 | 输入真实账号密码或授权码 |
| 生成 Windows 构建包 | 在真实 Windows 电脑上重启验证开机启动 |
| 根据文档拆分任务 | 决定需求优先级是否要调整 |

### 4.6 进度管理文件

项目进度由三个文件共同管理：

| 文件 | 用途 | 更新时机 |
|---|---|---|
| `todo.md` | 唯一任务来源，记录任务拆分、依赖、工时和验收标准 | 任务范围、依赖或阶段计划变化时 |
| `progress.md` | 记录每日推进、完成内容、待验收项和文档同步情况 | 每次完成任务、阶段验收或当天结束时 |
| `risk.md` | 记录风险、阻塞、影响范围和下一步处理 | 出现阻塞、连续报错、外部依赖不确定时 |

`todo.md` 不记录日常流水，只维护任务本身；`progress.md` 记录“今天做了什么”；`risk.md` 记录“什么可能影响继续推进”。这样可以避免任务清单变得混乱，也方便 TRAE 在后续会话中快速恢复项目状态。

### 4.7 每日推进流程

每天开始时，让 TRAE 先读进度和风险：

```text
请读取 todo.md、progress.md 和 risk.md。
告诉我当前应该继续哪个任务、是否存在阻塞、今天建议完成哪些内容。
不要修改代码。
```

每天结束时，让 TRAE 写入当天进度：

```text
请根据今天完成的内容更新 progress.md。
如果存在阻塞或风险，请同步更新 risk.md。
同时检查 requirements.md、design.md、todo.md 是否需要同步修改。
```

### 4.8 阶段结束流程

每完成一个 Phase 后，执行一次阶段验收：

```text
当前 Phase 已完成。
请读取 requirements.md、design.md、todo.md、progress.md、risk.md 和 GUIDE.md。
检查该 Phase 的验收标准是否都已满足。
如果发现文档不一致，请先修正文档；如果发现风险未关闭，请更新 risk.md。
不要新增功能。
```

---

## 五、所需能力与资源清单

### 5.1 你需要具备的能力

| 能力 | 要求程度 | 说明 |
|---|---|---|
| **基础电脑操作** | 必须 | 会安装软件、打开终端、复制粘贴命令 |
| **英语阅读** | 基础 | 能看懂简单的软件安装界面和报错提示 |
| **逻辑思维** | 必须 | 能判断"这个功能是否符合我的预期" |
| **耐心** | 必须 | TRAE 可能需要多轮调试，遇到问题不急躁 |
| **Git 基础** | 推荐 | 会 `git add`、`git commit`、`git push`，用于备份代码 |
| **Markdown 阅读** | 推荐 | 能看懂 `requirements.md` 和 `design.md` 的结构 |

### 5.2 你不需要具备的能力

以下能力可以交给 TRAE，你不需要会：

- 编写 TypeScript / Rust 代码
- 理解 SQL 语法
- 配置 Webpack / Vite
- 理解 HTTP 协议细节
- 操作 Windows 注册表
- 理解 WebDAV 协议

### 5.3 外部资源清单

| 资源 | 用途 | 获取方式 |
|---|---|---|
| **TRAE** | 编写代码、调试、答疑、读写文件、运行命令 | 当前已具备 |
| **Windows 电脑** | 开发、测试、运行 | 自备 |
| **坚果云账号** | WebDAV 同步测试 | 免费注册 |
| **备用测试电脑** | 干净环境测试 | 借用或虚拟机 |
| **节假日数据源** | 万年历数据更新 | 由 TRAE 帮你对接中国大陆官方公开数据 |

### 5.4 时间投入预估

| 阶段 | 天数 | 每天投入 |
|---|---|---|
| Phase 1：环境搭建 | 1-2 天 | 2-3 小时 |
| Phase 2-3：框架 + 平台 | 5-6 天 | 2-3 小时 |
| Phase 4-5：业务 + 高级功能 | 5-6 天 | 2-3 小时 |
| Phase 6：测试发布 | 2 天 | 2-3 小时 |
| **合计** | **约 15 天** | **约 30-45 小时** |

> 注：这是非编程专业人员的保守估计。每天投入时间越多，进度越快。如果某天 TRAE 一直解决不了同一个问题，可以先让它把问题缩小为最小可复现范围，第二天带着更清晰的上下文继续。

---

## 六、风险预案

### 6.1 TRAE 多次修复失败的情况

| 情况 | 你的应对 |
|---|---|
| TRAE 连续 3 次修复同一报错 | 要求 TRAE 停止继续试错，先总结已尝试方案，再把问题简化为最小可复现示例 |
| 需要付费 API / 云服务 | 评估是否必要，非核心功能可延后或砍掉 |
| 某个技术方案实在跑不通 | 让 TRAE 提供替代方案（如 SQLite 换成 JSON 文件），并同步更新 `design.md` |
| 代码量太大导致上下文混乱 | 让 TRAE 只处理一个模块或一个文件，并把大任务拆成更小的子任务 |
| 修改后文档和代码不一致 | 要求 TRAE 先对比 `requirements.md`、`design.md`、`todo.md` 与代码差异，再统一修正 |

### 6.2 代码备份策略

**必须执行**：每完成一个 Phase，让 TRAE 帮你执行：

```bash
git add .
git commit -m "完成 Phase X"
```

如果有 GitHub 账号，再执行：
```bash
git push origin main
```

这样即使本地出问题，代码也不会丢失。

---

## 七、里程碑检查点

每完成一个 Phase，对照以下清单确认是否达标：

### Phase 1 完成标准
- [ ] `npm run tauri dev` 能打开一个空白窗口
- [ ] 项目目录结构与 `design.md` 一致
- [ ] SQLite 可以执行简单查询

### Phase 2 完成标准
- [ ] 模块系统可以注册/加载一个测试模块
- [ ] 配置可以读写并持久化
- [ ] 日志文件生成在数据目录中

### Phase 3 完成标准
- [ ] 数据目录在 exe 同级自动创建
- [ ] Windows 通知可以弹出
- [ ] 托盘图标可以显示和交互
- [ ] 可以连接坚果云 WebDAV

### Phase 4 完成标准
- [ ] 万年历显示正确农历和节假日
- [ ] 倒班助手可以创建班次模板、轮班周期，并在日历中叠加显示
- [ ] 可以添加/编辑/删除事项
- [ ] 事项数据关闭应用后保留
- [ ] WebDAV 同步可以上传和下载数据

### Phase 5 完成标准
- [ ] 可以启用/禁用模块
- [ ] 桌面小部件显示今日信息
- [ ] 开机启动生效

### Phase 6 完成标准
- [ ] 单文件 exe 可以在干净电脑运行
- [ ] 六大场景全部验证通过
- [ ] 代码注释符合 TRAE 后续维护规范

---

## 八、快速参考：常用 TRAE 提示词

### 开始新任务
```
请帮我执行 todo.md 中的 Task-XX：任务名称。
开始前请先读取 requirements.md、design.md、todo.md、progress.md、risk.md 中相关内容。
请直接修改项目文件，并在完成后运行测试或构建命令。
最后请说明修改了哪些文件、如何验证，并更新 progress.md。
如果遇到阻塞或风险，请更新 risk.md。
```

### 遇到报错
```
执行 [命令] 时报错，完整信息如下：
[粘贴报错]

请读取相关文件，定位原因，直接修复代码。
修复后请重新运行对应命令验证。
如果同一问题已经多次出现，请更新 risk.md，并给出替代方案。
```

### 功能验收不通过
```
我测试了 XXX 功能，预期是 [描述预期]，实际是 [描述实际]。
这不符合 requirements.md 中的 F-XXX 需求。
请先确认需求与设计是否一致，再修改代码。
如果需要同步更新文档，请先改文档再改代码。
完成后请更新 progress.md；如果该问题可能再次出现，请更新 risk.md。
```

### 让 TRAE 解释代码
```
请读取 [文件路径]，解释这段代码的作用，以及为什么要这样设计。
请重点说明：它对应 requirements.md 中的哪些需求，后续修改时有哪些风险。
```

### 让 TRAE 检查文档一致性
```
请检查 requirements.md、design.md、todo.md、GUIDE.md、progress.md、risk.md 是否存在不一致。
如果发现不一致，请列出差异，并直接修正文档。
不要修改代码。
```

### 让 TRAE 开始一个阶段
```
请开始 Phase X。
先读取 todo.md 中 Phase X 的全部任务，再检查 requirements.md、design.md、progress.md 和 risk.md 是否足够支撑开发。
如果文档缺失，请先补全文档；如果文档完整，再从第一个未完成任务开始实施。
```

### 每日开始
```
请读取 todo.md、progress.md 和 risk.md。
请告诉我当前项目状态、今天建议继续哪个任务、是否存在阻塞。
不要修改代码。
```

### 每日结束
```
请根据今天完成的内容更新 progress.md。
如果存在阻塞、兼容性问题或重复报错，请同步更新 risk.md。
同时检查 requirements.md、design.md、todo.md 是否需要同步修改。
```

### 阶段验收
```
当前 Phase X 已完成。
请读取 requirements.md、design.md、todo.md、progress.md、risk.md、GUIDE.md。
请检查该阶段的验收标准是否满足，并列出未满足项。
如果文档不一致，请先修正文档；不要新增功能。
```

---

> **最后提醒**：这个项目完全可行。你不需要会编程，只需要会描述需求、会验证结果、会反馈问题。TRAE 是你的程序员和调试助手，你是产品经理、测试工程师和最终用户。保持耐心，逐任务推进，15 天内你会拥有一个自己命名的桌面应用。
