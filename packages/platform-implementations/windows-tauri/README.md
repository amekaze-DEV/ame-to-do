# packages/platform-implementations/windows-tauri

Windows + Tauri 平台适配器实现。实现 `packages/platform-contracts` 中定义的所有接口。

## 目录说明

- `src/` - 各平台能力适配器实现

## 依赖规则

仅可依赖 `packages/platform-contracts` 和 `packages/shared`。禁止依赖业务 UI。
