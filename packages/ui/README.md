# packages/ui

跨端通用 UI 组件库。包含 Button、Dialog、Form、List 等基础组件，响应式布局、导航、分栏，主题、DPI、字号、间距配置和图标组件。

## 目录说明

- `src/components/` - Button、Dialog、Form、List 等基础组件
- `src/layout/` - 响应式布局、导航、分栏
- `src/theme/` - 主题、DPI、字号、间距
- `src/icons/` - 图标组件

## 依赖规则

仅可依赖 `packages/shared`。禁止依赖业务服务、平台实现。
