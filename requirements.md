# AME to do APP 需求文档 PRD

## 文档信息

| 项目 | 内容 |
|---|---|
| 项目名称 | AME to do |
| 文档类型 | APP 产品需求文档 PRD |
| 初始目标平台 | Windows |
| 预留平台 | macOS、Android、iOS、Linux、鸿蒙系统 |
| 目标用户 | 倒班工人、工厂管理层 |
| 核心能力 | 万年历、倒班助手、事项管理、本地运行、WebDAV 跨端同步、模块化扩展、模块开关、开机启动、桌面小部件、单文件便携封装 |
| 文档状态 | 初版需求草案 |

## 产品定位

AME to do 是一款面向倒班工人及工厂管理层的本地优先型效率工具。产品初始版本以 Windows 桌面端为入口，提供万年历查询、倒班助手、事项管理、WebDAV 跨端同步、模块开关、开机启动和桌面小部件能力，并为后续扩展排班、班组协作、工厂通知、统计看板等模块预留架构接口。

产品采用本地运行优先的设计，并以单文件便携封装为目标。最终交付物为一个可执行文件，运行时在同级目录自动生成数据目录，用户无需安装即可使用。用户在无网络或弱网络环境下仍可查看日历、管理待办和维护定时事项；当网络恢复并配置 WebDAV 服务后，系统将用户数据同步到 WebDAV 存储空间，并支持多设备之间的数据一致性。

## 目标用户

### 倒班工人

倒班工人的工作时间不固定，常见需求包括查看节假日与调休安排、记录班次相关事项、设置提醒、跨设备查看待办事项。该类用户更关注操作简单、离线可用、提醒可靠和数据不丢失。

### 工厂管理层

工厂管理层需要查看日期、节假日、调休信息，并对生产安排、人员沟通、现场检查、会议、巡检等事项建立提醒。该类用户更关注事项管理效率、优先级排序、跨设备同步和后续扩展到班组管理或生产协同的可能性。

## 产品目标

1. 支持用户在 Windows 本地稳定运行，不依赖云服务即可使用核心功能。
2. 支持用户通过 WebDAV 同步数据，实现多端之间的数据备份与跨端同步。
3. 提供万年历模块，支持公历、农历、节假日、调休信息查询、实时更新和倒班助手。
4. 提供统一事项管理模块，支持待办事项与定时事项的新增、删除、编辑、优先级调整、提醒和同步。
5. 采用模块化架构，支持模块开关，未来可拓展功能模块。
6. 为 macOS、Android、iOS、Linux、鸿蒙系统预留移植接口，降低后续跨平台开发成本。
7. 在代码、配置、数据结构和模块边界中加入更详尽注释，方便 AI Agent 后续更新、调试和 Debug。
8. 以单文件可执行文件为交付目标，运行时自动创建运行目录，支持开机启动和桌面小部件。
9. Windows 平台提供系统托盘图标、右键菜单、开机启动、桌面小部件等桌面端增强能力。

## 产品范围

### 初始版本包含

| 模块 | 功能范围 |
|---|---|
| 应用基础框架 | 本地运行、模块加载、配置管理、数据存储、日志记录、单文件封装 |
| 万年历 | 公历查询、农历查询、节假日查询、调休查询、日期跳转、信息更新、倒班助手 |
| 事项管理 | 待办事项、定时事项、新增、删除、编辑、完成状态、优先级调整、提醒规则、列表筛选 |
| WebDAV 同步 | 账号配置、连接测试、上传、下载、冲突处理、同步状态展示 |
| 模块开关管理 | 模块启用与禁用、模块列表展示、依赖检测、开关状态同步 |
| 开机启动 | 系统开机自启动配置、启动时自动同步、启动后最小化到托盘 |
| Windows 桌面小部件 | 今日日期、今日农历、今日待办、即将提醒的 Windows 桌面悬浮展示，支持标准化扩展接口 |
| 跨平台预留 | 平台适配层、文件系统适配、通知接口预留、同步接口抽象 |
| AI Agent 维护支持 | 注释规范、模块说明、调试日志、错误码、接口文档 |

### 初始版本暂不包含

| 功能 | 说明 |
|---|---|
| 企业账号系统 | 初始版本以本地用户和 WebDAV 配置为主，不内置中心化账号体系 |
| 班组排班协作 | 可作为未来扩展模块，不进入初始版本核心范围 |
| 多人实时协同 | 初始版本聚焦个人数据同步，不处理多人同时编辑 |
| 云端自建服务器 | WebDAV 作为同步存储目标，产品不强制提供自有云服务 |
| 复杂审批流 | 可作为工厂管理模块的后续能力 |

## 核心使用场景

### 场景一：倒班工人查看节假日和调休

用户打开 AME to do 后进入万年历模块，系统默认展示当前月份。用户可以查看当天对应的公历、农历、星期、节假日名称和调休状态。用户也可以跳转到指定日期，查询未来节假日和调休安排，用于提前规划请假、倒班、出行或家庭事务。

### 场景二：倒班工人设置轮班周期

用户在万年历模块中打开倒班助手，创建“白班、夜班、休息”的班次模板，并设置轮班周期，例如“白班 2 天、夜班 2 天、休息 2 天”。系统根据起始日期自动生成未来班次，并在月视图中叠加显示。用户可以临时调整某一天的班次，调整结果应优先于自动生成规则，并参与 WebDAV 同步。

### 场景三：倒班工人记录个人待办

用户在事项管理模块中新增待办事项，例如“夜班前检查工具”“周五提交请假申请”“月底体检”。系统允许用户设置事项标题、备注、优先级、截止时间、提醒时间和完成状态。用户调整优先级后，列表顺序应立即变化，并在本地保存。

### 场景四：管理层维护生产相关定时事项

管理层可以在事项管理模块中创建定时事项，例如“每日 08:00 查看生产日报”“每周一检查安全记录”“每月 25 日汇总考勤异常”。系统根据提醒规则触发本地通知，并允许用户编辑、暂停、删除或完成该事项。

### 场景五：用户跨设备同步数据

用户在 Windows 端的系统设置中配置 WebDAV 账号后，系统将本地事项、配置和同步元数据上传到 WebDAV。用户未来在其他平台登录同一 WebDAV 地址后，可以拉取同一份数据，实现跨端查看和继续编辑。

### 场景六：倒班工人使用桌面小部件快速查看信息

用户将 AME to do 的桌面小部件固定在桌面角落。小部件实时展示今日日期、农历、节假日状态，以及当日待办事项和即将到期的提醒。用户无需打开主应用即可快速获取关键信息，点击小部件上的具体事项可直接跳转至主应用对应模块。

### 场景七：管理层设置开机启动，确保提醒不遗漏

管理层在系统设置中开启"开机启动"选项。每次 Windows 启动后，AME to do 自动在后台运行并最小化到系统托盘。定时事项到期时，系统通过桌面通知提醒用户；桌面小部件同步更新当日待办和提醒状态，管理层不会错过生产检查、安全巡检等关键事项。

## 信息架构

```text
AME to do
├── 首页
│   ├── 今日日期
│   ├── 今日农历
│   ├── 今日节假日与调休状态
│   ├── 今日待办
│   └── 即将提醒
├── 万年历
│   ├── 月视图
│   ├── 日期详情
│   ├── 节假日信息
│   ├── 倒班助手
│   │   ├── 班次模板
│   │   ├── 轮班周期
│   │   ├── 班次日历叠加
│   │   └── 临时班次调整
│   └── 数据更新状态
├── 事项管理
│   ├── 事项列表
│   ├── 新增待办事项
│   ├── 新增定时事项
│   ├── 编辑事项
│   ├── 优先级调整
│   ├── 提醒与重复规则
│   └── 完成、暂停与删除
├── 模块管理
│   ├── 模块列表
│   ├── 模块开关
│   └── 模块依赖提示
├── 桌面小部件
│   ├── 今日概览
│   ├── 待办列表
│   └── 即将提醒
└── 系统设置
    ├── 本地数据路径
    ├── 通知设置
    ├── 同步设置
    │   ├── WebDAV 地址
    │   ├── 用户名
    │   ├── 密码或授权码
    │   ├── 连接测试
    │   ├── 手动同步
    │   └── 同步状态
    ├── 开机启动设置
    ├── 桌面小部件设置
    ├── 模块开关设置
    ├── 日志设置
    ├── 数据导入导出
    └── 关于应用
```

## 功能需求

## 模块一：应用基础框架

### 功能说明

应用基础框架负责承载所有业务模块，包括模块注册、模块加载、模块开关管理、配置读取、本地数据访问、日志记录、异常处理和跨平台能力抽象。初始版本虽然只包含万年历、事项管理、WebDAV 同步、模块开关管理、开机启动和桌面小部件，但框架必须允许后续新增模块，而不破坏现有模块。

系统启动时应加载基础配置、本地数据库或本地数据文件、已启用模块列表和同步状态。框架应支持每个模块独立启用或禁用，被禁用的模块不应加载其 UI、不占用运行时资源、不参与同步，但其数据仍应保留在本地，以便用户重新开启时恢复。

若某个模块加载失败，系统不能直接整体崩溃，应记录错误并展示可理解的错误信息。后续 AI Agent 或开发者新增模块时，应只需遵循模块接口规范，即可将新模块接入主应用。

单文件封装是 Windows 初始版本的交付目标。最终发布物为一个可执行文件，首次运行时在同级目录自动创建数据目录（例如 `./ame-to-do-data/`），用于保存配置、数据库、日志和缓存。用户可直接复制该可执行文件到任意位置运行，数据目录跟随可执行文件位置，实现便携性。

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-BASE-001 | 模块注册机制 | 每个功能模块需要声明模块名称、版本、入口、依赖项和启用状态 |
| F-BASE-002 | 模块独立性 | 万年历、事项管理、同步等模块之间不能直接强耦合，应通过统一服务或事件机制交互 |
| F-BASE-003 | 本地配置管理 | 应支持读取和保存用户配置，例如主题、通知、WebDAV 设置和本地数据路径 |
| F-BASE-004 | 本地数据存储 | 应支持离线保存用户数据，网络不可用时不影响核心功能 |
| F-BASE-005 | 日志记录 | 应记录启动、同步、提醒、数据读写、异常等关键日志 |
| F-BASE-006 | 错误恢复 | 模块异常时应给出错误提示，并尽量保留其他模块可用 |
| F-BASE-007 | 模块开关 | 用户可在系统设置或模块管理中独立启用或禁用每个功能模块 |
| F-BASE-008 | 单文件封装 | 最终交付物为单可执行文件，运行时自动创建同级数据目录 |
| F-BASE-009 | 数据目录自动创建 | 首次运行时检测同级目录是否存在数据目录，不存在则自动创建 |
| F-BASE-010 | 便携性 | 用户复制可执行文件到新位置后，数据目录应在新位置重新创建或跟随迁移 |
| F-BASE-011 | 系统托盘 | 最小化时支持缩放到系统托盘，托盘图标支持右键菜单 |
| F-BASE-012 | 模块数据保留 | 模块被禁用时，其历史数据保留在本地，重新启用后可恢复访问 |

### 模块扩展规则

新增模块应至少包含以下信息：

| 字段 | 说明 |
|---|---|
| moduleId | 模块唯一标识，例如 `calendar`、`todo`、`sync` |
| moduleName | 模块显示名称 |
| version | 模块版本号 |
| enabled | 模块是否启用，默认 true |
| dependencies | 依赖模块列表 |
| entry | 模块入口 |
| permissions | 模块所需权限，例如本地文件、通知、网络 |
| dataSchemaVersion | 模块数据结构版本 |

## 模块二：万年历

### 功能说明

万年历模块为用户提供公历、农历、节假日、调休信息查询和倒班助手。用户进入模块后，默认展示当前月份，并突出显示当天日期。用户可以切换月份、跳转年份、点击某一天查看日期详情。日期详情中应包含公历日期、农历日期、星期、节气、节假日、是否调休、是否工作日、当日班次等信息。

节假日和调休数据采用中国大陆官方公开数据。系统应允许通过内置规则、本地缓存和远程更新三种方式维护日历数据。无网络时，用户仍可查看已缓存的节假日和调休信息；有网络时，系统可检查更新并提示用户是否刷新数据。若远程数据不可用，系统应保留上一份可用数据，并在界面上提示数据更新时间。

倒班助手用于帮助倒班工人维护个人轮班规律。用户可以创建班次模板，例如白班、夜班、中班、休息；也可以设置轮班周期，例如“白班 2 天、夜班 2 天、休息 2 天”。系统根据起始日期和周期规则自动生成未来班次，并在万年历月视图和日期详情中叠加显示。用户可对某一天进行临时班次调整，临时调整优先于自动生成规则。

### 页面布局

| 区域 | 内容 |
|---|---|
| 顶部区域 | 当前年月、返回今日、月份切换、年份跳转 |
| 日历主体 | 按周展示日期格，显示公历日期、农历简写、节假日标记、调休标记、班次标记 |
| 日期详情 | 展示选中日期的完整信息和当日班次 |
| 倒班助手 | 管理班次模板、轮班周期、起始日期和临时班次调整 |
| 更新状态 | 展示节假日数据版本、最后更新时间和更新入口 |

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-CAL-001 | 月视图展示 | 默认展示当前月份，支持上月、下月切换 |
| F-CAL-002 | 今日高亮 | 当前日期需要有明显视觉标识 |
| F-CAL-003 | 日期跳转 | 支持用户输入年份和月份后跳转 |
| F-CAL-004 | 公历信息 | 日期详情必须展示公历年月日和星期 |
| F-CAL-005 | 农历信息 | 日期详情必须展示农历年月日，必要时展示节气 |
| F-CAL-006 | 节假日信息 | 日期格和详情页需要展示节假日名称 |
| F-CAL-007 | 调休信息 | 日期格和详情页需要标识调休上班、补班、休息 |
| F-CAL-008 | 实时更新 | 支持基于中国大陆官方公开数据检查节假日和调休数据更新 |
| F-CAL-009 | 本地缓存 | 无网络时使用本地缓存数据 |
| F-CAL-010 | 数据版本 | 展示节假日数据最后更新时间或版本号 |
| F-CAL-011 | 班次模板 | 用户可创建、编辑、删除班次模板，例如白班、夜班、中班、休息 |
| F-CAL-012 | 轮班周期 | 用户可设置轮班周期，例如白班 2 天、夜班 2 天、休息 2 天 |
| F-CAL-013 | 起始日期 | 用户可设置轮班周期的起始日期，系统从起始日期开始推算班次 |
| F-CAL-014 | 班次日历叠加 | 月视图日期格应显示当日班次标记 |
| F-CAL-015 | 日期详情班次 | 日期详情中展示当日班次、班次时间和备注 |
| F-CAL-016 | 临时班次调整 | 用户可修改某一天班次，临时调整优先于周期规则 |
| F-CAL-017 | 班次提醒联动 | 用户可为班次设置上班前提醒，提醒进入事项或通知系统 |
| F-CAL-018 | 倒班数据同步 | 班次模板、轮班周期和临时调整应支持 WebDAV 跨端同步 |
| F-CAL-019 | 多周期预览 | 用户可预览未来至少 3 个月的班次安排 |
| F-CAL-020 | 班次颜色标记 | 不同班次可设置不同颜色或标签，便于在日历中区分 |

### 边界情况

| 情况 | 系统处理 |
|---|---|
| 节假日数据更新失败 | 保留旧数据，提示用户更新失败原因 |
| 用户查询超远年份 | 若数据无法覆盖，应展示公历和基础农历信息，并提示节假日数据不可用 |
| 本地缓存损坏 | 尝试恢复默认数据，记录日志，并提示用户重新更新 |
| 网络不可用 | 禁用实时更新入口或提示离线状态，不影响本地查询 |
| 轮班周期配置不完整 | 阻止保存，并提示用户补全班次、天数和起始日期 |
| 临时调整与周期规则冲突 | 以临时调整为准，并在日期详情中标记“已手动调整” |
| 班次跨天 | 允许夜班等班次结束时间晚于开始日期，并在次日详情中提示跨天班次 |
| 多端同时修改班次规则 | 标记同步冲突，允许用户保留本地、使用远程或保留两份 |

## 模块三：事项管理

### 功能说明

事项管理模块统一承载待办事项和定时事项。用户可以在同一入口中新增、删除、编辑事项，也可以设置事项类型、优先级、截止时间、提醒时间、重复规则、备注和完成状态。普通待办强调任务完成，定时事项强调在指定时间触发提醒，两者使用统一列表、统一编辑入口和统一同步逻辑，减少用户在多个模块之间切换。

事项数据需要参与 WebDAV 同步。系统在本地生成或修改事项后，应记录数据变更时间、设备标识和同步状态。同步模块执行时，将事项数据上传到 WebDAV；其他设备拉取数据后，应根据冲突处理规则合并或提示用户处理。

### 页面布局

| 区域 | 内容 |
|---|---|
| 筛选区域 | 全部、待办事项、定时事项、未完成、已完成、高优先级、今日到期 |
| 列表区域 | 事项标题、事项类型、优先级、截止时间、提醒状态、重复规则、完成状态 |
| 操作区域 | 新增待办、新增定时事项、编辑、删除、完成、暂停、调整优先级 |
| 同步状态 | 展示待同步、同步中、已同步、冲突等状态 |

### 字段规则

| 字段 | 类型 | 必填 | 规则 | 默认值 |
|---|---|---|---|---|
| id | 字符串 | 是 | 系统生成，全局唯一 | 自动生成 |
| itemType | 枚举 | 是 | 待办事项、定时事项 | 待办事项 |
| title | 文本 | 是 | 建议 1-100 个字符 | 无 |
| description | 文本 | 否 | 支持长文本备注 | 空 |
| priority | 枚举 | 是 | 高、中、低 | 中 |
| dueAt | 日期时间 | 否 | 截止时间不能早于创建时间过多，异常时提示确认 | 空 |
| remindAt | 日期时间 | 否 | 提醒时间应早于或等于截止时间 | 空 |
| repeatRule | 枚举或规则对象 | 否 | 不重复、每天、每周、每月、自定义；仅定时事项必须关注 | 不重复 |
| enabled | 布尔 | 是 | 定时事项是否启用提醒，普通待办默认启用提醒能力 | 是 |
| lastTriggeredAt | 日期时间 | 否 | 最近一次提醒触发时间 | 空 |
| nextTriggerAt | 日期时间 | 否 | 下一次提醒时间 | 根据规则计算 |
| status | 枚举 | 是 | 未完成、已完成 | 未完成 |
| createdAt | 日期时间 | 是 | 系统生成 | 当前时间 |
| updatedAt | 日期时间 | 是 | 每次修改时更新 | 当前时间 |
| syncStatus | 枚举 | 是 | 未同步、同步中、已同步、冲突、失败 | 未同步 |

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-ITEM-001 | 新增待办事项 | 用户可输入标题、备注、优先级、截止时间和提醒时间 |
| F-ITEM-002 | 新增定时事项 | 用户可设置标题、备注、提醒时间、重复规则和优先级 |
| F-ITEM-003 | 编辑事项 | 用户可编辑标题、备注、类型、优先级、截止时间、提醒时间、重复规则和完成状态 |
| F-ITEM-004 | 删除事项 | 用户可删除待办事项或定时事项，删除前需要二次确认 |
| F-ITEM-005 | 完成事项 | 用户可将事项标记为已完成，也可恢复为未完成 |
| F-ITEM-006 | 启用或暂停提醒 | 用户可暂停定时事项，暂停后不再触发提醒 |
| F-ITEM-007 | 到期提醒 | 到达提醒时间后，系统应通过本地通知提醒用户 |
| F-ITEM-008 | 稍后提醒 | 用户可选择稍后提醒，例如 5 分钟、15 分钟、1 小时后 |
| F-ITEM-009 | 重复计算 | 重复事项触发后，系统应计算下一次提醒时间 |
| F-ITEM-010 | 优先级调整 | 用户可设置高、中、低优先级，列表按规则重新排序 |
| F-ITEM-011 | 筛选列表 | 支持按事项类型、状态、优先级、到期时间筛选 |
| F-ITEM-012 | 本地保存 | 所有操作应立即写入本地数据 |
| F-ITEM-013 | 同步标记 | 本地变更后，事项状态应变为未同步 |
| F-ITEM-014 | 冲突提示 | 同一事项在多端修改后，系统应识别冲突并给出处理方式 |

### 优先级排序规则

| 排序条件 | 优先级 |
|---|---|
| 未完成事项 | 高于已完成事项 |
| 高优先级 | 高于中、低优先级 |
| 截止时间更近 | 高于截止时间更远 |
| 更新时间更近 | 在其他条件相同时更靠前 |

### 边界情况

| 情况 | 系统处理 |
|---|---|
| 应用未运行时错过提醒 | 下次启动时检测错过提醒，并按用户设置展示补提醒 |
| 系统时间被修改 | 记录时间异常日志，重新计算下一次提醒时间 |
| 重复规则无效 | 阻止保存，并提示用户修改规则 |
| 通知权限不可用 | 提示用户开启通知权限，同时保留应用内提醒 |

## 模块四：WebDAV 同步

### 功能说明

WebDAV 同步模块负责将用户本地数据同步到用户指定的 WebDAV 服务。系统设置中应提供 WebDAV 配置入口，支持配置 WebDAV 地址、用户名、密码或授权码，并提供连接测试能力。连接成功后，系统在 WebDAV 中创建或使用指定应用目录，用于保存 AME to do 的用户数据、同步元数据和必要的备份文件。

同步机制应以本地数据为基础，支持手动同步和自动同步。用户未配置 WebDAV 时，应用仍可完整使用本地功能。用户配置 WebDAV 后，系统应在新增、编辑、删除事项后标记数据变更，并在合适时机上传。其他设备拉取数据时，应根据更新时间、设备 ID 和数据版本判断是否需要合并。

WebDAV 兼容范围应覆盖国内主流 WebDAV 服务和用户自建 WebDAV 服务器。产品不应绑定某个单一服务商，连接测试和错误提示需要能够区分地址不可达、认证失败、目录无权限、服务端不支持标准方法等情况。

### 同步范围

| 数据类型 | 是否同步 | 说明 |
|---|---|---|
| 事项数据 | 是 | 包括待办事项和定时事项的标题、备注、优先级、时间、状态、提醒规则 |
| 倒班数据 | 是 | 包括班次模板、轮班周期、起始日期、临时班次调整和班次提醒配置 |
| 万年历缓存 | 可选 | 节假日缓存可同步，也可由各端独立更新 |
| 用户配置 | 部分同步 | WebDAV 密码不应明文跨端同步 |
| 同步元数据 | 是 | 包括设备 ID、数据版本、更新时间 |
| 日志文件 | 默认不同步 | 日志可能包含设备信息，默认仅保存在本地 |

### WebDAV 配置字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| serverUrl | 文本 | 是 | WebDAV 服务地址 |
| username | 文本 | 是 | WebDAV 用户名 |
| passwordOrToken | 密文 | 是 | 密码或授权码，必须本地加密保存 |
| remotePath | 文本 | 否 | 远程目录，默认 `/AME-to-do/` |
| syncMode | 枚举 | 是 | 手动、启动时、定时、数据变更后 |
| lastSyncAt | 日期时间 | 否 | 最近一次同步时间 |

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-SYNC-001 | WebDAV 配置 | 用户可输入 WebDAV 地址、用户名、密码或授权码 |
| F-SYNC-002 | 连接测试 | 保存前应支持测试连接并展示结果 |
| F-SYNC-003 | 本地优先 | 未配置 WebDAV 或网络不可用时，核心功能仍可使用 |
| F-SYNC-004 | 手动同步 | 用户可点击按钮立即同步 |
| F-SYNC-005 | 自动同步 | 支持启动时、定时或数据变更后自动同步 |
| F-SYNC-006 | 上传数据 | 本地变更后可上传到 WebDAV |
| F-SYNC-007 | 下载数据 | 新设备可从 WebDAV 拉取远程数据 |
| F-SYNC-008 | 冲突识别 | 同一条数据在不同设备修改后，应识别冲突 |
| F-SYNC-009 | 冲突处理 | 支持保留本地、使用远程、保留两份或按更新时间自动合并 |
| F-SYNC-010 | 同步状态展示 | 展示未同步、同步中、已同步、冲突、失败等状态 |
| F-SYNC-011 | 凭据安全 | 密码或授权码必须加密保存在本地，不得明文写入同步文件 |
| F-SYNC-012 | 失败重试 | 网络异常或服务异常时，应支持稍后重试 |
| F-SYNC-013 | 兼容国内主流服务 | WebDAV 同步需兼容国内主流 WebDAV 服务和自建 WebDAV 服务器 |

### 同步状态流转

```text
本地新增或修改
  ↓
未同步
  ↓ 用户手动同步或系统自动同步
同步中
  ↓
成功 → 已同步
失败 → 同步失败
冲突 → 等待用户处理
```

### 冲突处理规则

| 场景 | 默认处理 | 用户可选操作 |
|---|---|---|
| 本地新建，远程不存在 | 上传本地数据 | 无需用户处理 |
| 远程新建，本地不存在 | 下载远程数据 | 无需用户处理 |
| 本地和远程均修改同一事项 | 标记冲突 | 保留本地、使用远程、保留两份 |
| 本地删除但远程修改 | 标记冲突 | 确认删除、恢复远程、保留两份 |
| 远程删除但本地修改 | 标记冲突 | 上传本地、确认删除、保留两份 |

## 模块五：模块开关管理

### 功能说明

模块开关管理允许用户独立启用或禁用每个功能模块。用户可在系统设置或专门的模块管理页面中查看所有已注册模块的列表，并通过开关控制每个模块的启用状态。被禁用的模块不加载其 UI、不占用运行时资源、不参与同步，但模块历史数据保留在本地，用户重新开启后可立即恢复。

模块开关需要处理依赖关系。若模块 A 依赖模块 B，禁用模块 B 时应提示用户模块 A 也将被影响，或自动禁用模块 A。启用某个模块时，若其依赖模块未启用，应提示用户先启用依赖模块。

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-MOD-001 | 模块列表展示 | 展示所有已注册模块的名称、版本、描述和当前启用状态 |
| F-MOD-002 | 模块开关 | 用户可通过开关启用或禁用单个模块 |
| F-MOD-003 | 依赖检测 | 禁用某个模块时，检测并提示受影响的依赖模块 |
| F-MOD-004 | 数据保留 | 禁用模块时保留本地数据，重新启用后恢复访问 |
| F-MOD-005 | 同步状态 | 模块开关状态应参与配置同步，多端保持一致 |
| F-MOD-006 | 默认启用 | 初始安装时，核心模块（万年历、事项管理）默认启用 |

### 边界情况

| 情况 | 系统处理 |
|---|---|
| 禁用正在使用的模块 | 关闭该模块的 UI 页面，保存当前状态，提示用户数据已保留 |
| 依赖模块被禁用 | 提示用户哪些模块将受影响，提供一键禁用关联模块或取消操作 |
| 模块开关状态同步冲突 | 以最后操作时间为准，或提示用户选择 |

## 模块六：开机启动

### 功能说明

开机启动模块允许用户配置 AME to do 在 Windows 系统启动时自动运行。开启后，每次用户登录 Windows 时，应用自动在后台启动并最小化到系统托盘。启动后可根据配置自动执行 WebDAV 同步，确保用户开机后即可获取最新数据。

开机启动的实现应通过 Windows 注册表或启动文件夹完成，并支持用户随时在系统设置中关闭。应用启动后默认最小化到托盘，不主动弹出主窗口，避免打扰用户。

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-AUTO-001 | 开机启动开关 | 用户可在系统设置中开启或关闭开机启动 |
| F-AUTO-002 | 启动后最小化 | 开机启动后应用自动最小化到系统托盘，不弹出主窗口 |
| F-AUTO-003 | 启动后同步 | 支持配置启动后是否自动执行一次 WebDAV 同步 |
| F-AUTO-004 | 启动顺序 | 开机启动应在用户登录后尽快执行，但不影响系统启动速度 |
| F-AUTO-005 | 权限检测 | 设置开机启动时检测系统权限，必要时提示用户以管理员身份运行 |

### 边界情况

| 情况 | 系统处理 |
|---|---|
| 用户无管理员权限 | 提示用户当前设置可能无法生效，建议以管理员身份运行 |
| 开机启动后网络不可用 | 正常启动到托盘，同步失败时记录日志，网络恢复后自动重试 |
| 多用户系统 | 开机启动设置应绑定当前用户，不影响其他用户 |

## 模块七：桌面小部件

### 功能说明

初始版本的桌面小部件仅针对 Windows 平台设计，是悬浮在 Windows 桌面上的轻量级信息展示窗口。用户无需打开主应用即可查看关键信息。小部件默认展示今日日期、农历、节假日状态，以及当日待办事项和即将到期的提醒。用户可自定义小部件展示的内容、大小、位置和透明度。

小部件支持点击交互。点击日期区域可打开主应用的万年历模块，点击待办事项可打开事项管理模块并定位到对应事项。小部件应始终置顶显示，但允许用户设置是否在其他窗口全屏时隐藏。

桌面小部件需要具备可扩展和标准化接口特性。后续新增功能模块时，模块可以通过标准接口向小部件注册可展示卡片、数据来源、刷新频率和点击动作。Windows 桌面小部件负责承载这些标准卡片，但不直接写入具体业务规则。其他平台的同类能力，例如 macOS 菜单栏/桌面组件、Android 小组件、iOS Widget、Linux 桌面组件、鸿蒙服务卡片，应单独设计平台形态，并复用同一套小部件数据和注册接口。

### 需求明细

| 编号 | 需求 | 说明 |
|---|---|---|
| F-WIDGET-001 | 今日概览 | 展示今日公历日期、农历、星期、节假日名称 |
| F-WIDGET-002 | 待办展示 | 展示当日待办事项列表，支持按优先级排序 |
| F-WIDGET-003 | 提醒展示 | 展示即将到期的定时提醒 |
| F-WIDGET-004 | 点击跳转 | 点击小部件上的事项可直接打开主应用对应模块 |
| F-WIDGET-005 | 位置调整 | 用户可拖动小部件到桌面任意位置 |
| F-WIDGET-006 | 大小调整 | 支持多种预设尺寸，或用户自定义宽高 |
| F-WIDGET-007 | 透明度设置 | 支持调节小部件背景透明度 |
| F-WIDGET-008 | 内容自定义 | 用户可选择在小部件中展示哪些信息 |
| F-WIDGET-009 | 全屏隐藏 | 支持配置当其他应用全屏时自动隐藏小部件 |
| F-WIDGET-010 | 开机显示 | 支持配置开机后是否自动显示桌面小部件 |
| F-WIDGET-011 | Windows 平台限定 | 初始版本桌面小部件仅针对 Windows 平台实现，其他平台同类功能另行设计 |
| F-WIDGET-012 | 标准化小部件接口 | 提供统一的 WidgetExtension 接口，允许功能模块注册小部件内容 |
| F-WIDGET-013 | 模块卡片注册 | 后续模块可注册一个或多个小部件卡片，例如排班卡片、生产提醒卡片、统计卡片 |
| F-WIDGET-014 | 标准点击动作 | 小部件卡片需通过标准动作协议跳转主应用页面或执行模块动作 |
| F-WIDGET-015 | 标准刷新机制 | 小部件卡片需声明刷新策略，例如实时、定时、数据变更后刷新 |
| F-WIDGET-016 | 数据与窗口解耦 | 小部件数据由平台无关服务提供，Windows 悬浮窗口只负责展示和交互承载 |

### 边界情况

| 情况 | 系统处理 |
|---|---|
| 小部件被其他窗口遮挡 | 默认置顶显示，用户可配置是否允许被遮挡 |
| 待办事项过多 | 小部件内显示前 N 条，超出部分提示用户打开主应用查看 |
| 主应用未运行 | 小部件应独立运行，或提示用户启动主应用以获取最新数据 |
| 多显示器环境 | 小部件应支持在多显示器环境中选择显示在哪个屏幕 |
| 新模块注册卡片失败 | 保留原有小部件内容，记录失败原因，并在模块管理中提示该模块的小部件扩展不可用 |
| 其他平台请求小部件能力 | 不直接复用 Windows 悬浮窗形态，应基于平台特性单独设计，并复用标准数据接口 |

## 跨平台预留需求

### 设计目标

初始版本只支持 Windows 平台，但产品采用统一跨平台架构。后续迁移到 macOS、Android、iOS、Linux、鸿蒙系统时，业务模块不应大规模重写。平台差异应被封装在平台适配层中，例如文件路径、通知、开机启动、系统托盘、窗口管理、网络权限和安全存储。Windows 桌面小部件不作为其他平台的默认形态；其他平台的同类能力需要单独设计交互形态，但应复用标准小部件数据接口和模块扩展协议。

### 平台适配层

| 能力 | Windows 初始版本 | 跨平台预留 |
|---|---|---|
| 文件系统 | 支持本地数据目录读写 | 抽象文件读写接口 |
| 通知 | 支持 Windows 本地通知 | 抽象通知接口 |
| 安全存储 | 支持本地加密保存 WebDAV 凭据 | 抽象密钥存储接口 |
| 网络请求 | 支持 WebDAV 请求 | 抽象网络请求层 |
| 后台任务 | 支持定时同步和提醒检测 | 抽象任务调度接口 |
| 窗口管理 | 支持桌面窗口 | 移动端和桌面端分别适配 |
| 显示适配 | 支持 Windows 系统 DPI 缩放 | 抽象 DPI 缩放、横竖屏切换、不同长宽比自适应 |
| 日志路径 | Windows 本地日志目录 | 各平台独立日志目录 |

### 平台接口要求

| 编号 | 需求 | 说明 |
|---|---|---|
| F-PLAT-001 | 文件接口抽象 | 业务模块不得直接依赖某个平台的固定文件路径 |
| F-PLAT-002 | 通知接口抽象 | 定时提醒通过统一通知接口触发 |
| F-PLAT-003 | 凭据接口抽象 | WebDAV 密码或授权码通过统一安全存储接口保存 |
| F-PLAT-004 | 网络接口抽象 | WebDAV 请求通过统一网络层发起 |
| F-PLAT-005 | 任务调度抽象 | 自动同步和提醒检测通过统一调度接口执行 |
| F-PLAT-006 | UI 适配预留 | 页面布局需考虑桌面端和移动端差异 |
| F-PLAT-007 | 显示适配抽象 | 业务模块不得直接依赖固定像素尺寸或固定方向，应通过统一显示适配层获取 DPI、方向、可用宽高 |

## 数据结构需求

### 本地数据原则

1. 用户数据应优先保存在本地。
2. 本地数据应包含版本号，便于后续升级和迁移。
3. 每条可同步数据应包含唯一 ID、创建时间、更新时间、设备 ID 和同步状态。
4. 删除操作建议使用软删除标记，避免跨端同步时误恢复或误删除。
5. 数据结构变更时，应提供迁移逻辑和注释说明。

### 核心数据对象

```text
UserData
├── appConfig
├── calendarCache
├── shiftTemplates
├── shiftSchedules
├── shiftOverrides
├── items
├── syncMetadata
└── schemaVersion
```

### 统一事项对象

```json
{
  "id": "item_全局唯一ID",
  "itemType": "todo | timed",
  "title": "事项标题",
  "description": "备注",
  "priority": "high | medium | low",
  "dueAt": "截止时间，可为空",
  "remindAt": "提醒时间，可为空",
  "repeatRule": "none | daily | weekly | monthly | custom",
  "enabled": true,
  "lastTriggeredAt": "最近触发时间，可为空",
  "nextTriggerAt": "下一次触发时间",
  "status": "open | done",
  "createdAt": "创建时间",
  "updatedAt": "更新时间",
  "deletedAt": "删除时间，可为空",
  "deviceId": "最后修改设备ID",
  "syncStatus": "pending | syncing | synced | conflict | failed"
}
```

### 班次模板对象

```json
{
  "id": "shift_template_全局唯一ID",
  "name": "白班",
  "startTime": "08:00",
  "endTime": "20:00",
  "crossDay": false,
  "color": "#3B82F6",
  "description": "班次备注",
  "createdAt": "创建时间",
  "updatedAt": "更新时间",
  "deletedAt": "删除时间，可为空",
  "deviceId": "最后修改设备ID",
  "syncStatus": "pending | syncing | synced | conflict | failed"
}
```

### 轮班周期对象

```json
{
  "id": "shift_schedule_全局唯一ID",
  "name": "两白两夜两休",
  "startDate": "2026-07-22",
  "cycle": [
    { "shiftTemplateId": "白班模板ID", "days": 2 },
    { "shiftTemplateId": "夜班模板ID", "days": 2 },
    { "shiftTemplateId": "休息模板ID", "days": 2 }
  ],
  "enabled": true,
  "createdAt": "创建时间",
  "updatedAt": "更新时间",
  "deviceId": "最后修改设备ID",
  "syncStatus": "pending | syncing | synced | conflict | failed"
}
```

### 临时班次调整对象

```json
{
  "id": "shift_override_全局唯一ID",
  "date": "2026-07-25",
  "shiftTemplateId": "调整后的班次模板ID",
  "reason": "临时换班",
  "createdAt": "创建时间",
  "updatedAt": "更新时间",
  "deviceId": "最后修改设备ID",
  "syncStatus": "pending | syncing | synced | conflict | failed"
}
```

## 非功能需求

### 本地运行

| 编号 | 需求 | 说明 |
|---|---|---|
| NF-LOCAL-001 | 离线可用 | 无网络时可使用万年历和事项管理 |
| NF-LOCAL-002 | 启动速度 | 应避免启动时强依赖网络请求 |
| NF-LOCAL-003 | 数据安全 | 本地数据损坏时应尽量恢复，并保留错误日志 |
| NF-LOCAL-004 | 凭据保护 | WebDAV 密码或授权码必须加密保存 |
| NF-LOCAL-005 | 单文件便携 | 可执行文件应独立运行，数据目录自动创建在同级目录 |
| NF-LOCAL-006 | 数据目录隔离 | 数据目录与可执行文件分离，便于备份和迁移 |
| NF-LOCAL-007 | 资源占用控制 | 被禁用的模块不应占用运行时内存或 CPU 资源 |

### 同步可靠性

| 编号 | 需求 | 说明 |
|---|---|---|
| NF-SYNC-001 | 可重试 | 同步失败后可手动重试或自动重试 |
| NF-SYNC-002 | 可追踪 | 同步过程需要记录日志，便于定位失败原因 |
| NF-SYNC-003 | 不丢数据 | 冲突时不能静默覆盖用户数据 |
| NF-SYNC-004 | 状态可见 | 用户需要知道当前数据是否已同步 |

### 可维护性

| 编号 | 需求 | 说明 |
|---|---|---|
| NF-MAINT-001 | 模块边界清晰 | 每个模块应有独立目录、入口和说明 |
| NF-MAINT-002 | 注释详尽 | 关键业务逻辑、同步规则、数据迁移和平台适配必须写注释 |
| NF-MAINT-003 | 错误码明确 | 常见错误应有稳定错误码和说明 |
| NF-MAINT-004 | 日志可读 | 日志应包含模块名、操作、时间、结果和错误详情 |
| NF-MAINT-005 | 方便 AI Agent 修改 | 代码结构、命名和注释应能让 AI Agent 快速理解上下文 |

## AI Agent 维护与注释规范

### 注释目标

项目需要为 AI Agent 后续更新、功能新增、调试和 Debug 提供充分上下文。注释不是简单解释代码语法，而是说明业务意图、边界条件、数据来源、兼容性原因和修改风险。

### 必须注释的位置

| 位置 | 注释内容 |
|---|---|
| 模块入口 | 模块职责、依赖模块、初始化流程 |
| 数据模型 | 字段含义、取值范围、同步影响 |
| WebDAV 同步逻辑 | 上传、下载、冲突判断、重试策略 |
| 日历数据更新 | 数据来源、缓存策略、失败处理 |
| 定时提醒逻辑 | 重复规则、错过提醒、系统时间变化处理 |
| 平台适配层 | 平台差异、当前实现、未来移植注意事项 |
| 错误处理 | 错误原因、用户提示、恢复方式 |
| 数据迁移 | 旧版本结构、新版本结构、迁移风险 |

### 注释格式建议

```text
/**
 * 模块职责：
 * 说明当前模块解决什么问题，以及不负责什么问题。
 *
 * 业务规则：
 * 描述该函数或类背后的产品规则，例如同步冲突如何判断。
 *
 * 边界情况：
 * 描述网络失败、数据为空、权限不足、跨端冲突等情况。
 *
 * AI Agent 注意：
 * 说明后续修改时不能破坏的约束，以及相关联的模块。
 */
```

### README 要求

每个核心模块目录建议包含 `README.md`，至少说明以下内容：

| 内容 | 说明 |
|---|---|
| 模块职责 | 当前模块负责哪些功能 |
| 文件结构 | 关键文件及用途 |
| 对外接口 | 其他模块如何调用 |
| 数据结构 | 当前模块维护哪些数据 |
| 常见问题 | 调试时常见错误和排查方式 |
| 扩展方式 | 新增功能时应修改哪些位置 |

## 权限与安全需求

| 编号 | 需求 | 说明 |
|---|---|---|
| SEC-001 | WebDAV 凭据加密 | 用户密码、授权码不得明文保存 |
| SEC-002 | 本地数据访问控制 | 应避免将用户数据散落到不可控目录 |
| SEC-003 | 日志脱敏 | 日志中不得记录明文密码、授权码 |
| SEC-004 | 同步文件隔离 | WebDAV 中应使用独立应用目录，避免覆盖用户其他文件 |
| SEC-005 | 删除确认 | 删除事项、远程数据前应有明确确认 |
| SEC-006 | 开机启动权限 | 设置开机启动时应明确告知用户所需权限 |
| SEC-007 | 小部件权限 | 桌面小部件不应获取不必要的系统权限 |

## 异常与提示

| 场景 | 用户提示 | 系统处理 |
|---|---|---|
| WebDAV 地址不可访问 | WebDAV 连接失败，请检查地址或网络 | 保留配置，不执行同步 |
| WebDAV 账号错误 | 认证失败，请检查用户名或授权码 | 不保存无效凭据，或标记为待修正 |
| 同步冲突 | 当前事项在其他设备上被修改 | 展示冲突详情，等待用户选择 |
| 本地数据读取失败 | 本地数据异常，已尝试恢复 | 读取备份或创建新数据文件 |
| 通知不可用 | 当前系统通知不可用，请检查权限 | 保留应用内提醒 |
| 节假日更新失败 | 节假日数据更新失败，继续使用本地缓存 | 记录失败原因 |
| 轮班周期配置不完整 | 轮班规则不完整，请补全班次、天数和起始日期 | 阻止保存并定位到缺失字段 |
| 班次同步冲突 | 倒班规则在其他设备上被修改 | 展示本地和远程规则，等待用户选择 |
| 开机启动设置失败 | 开机启动设置失败，请检查权限或以管理员身份运行 | 保留用户配置，提示手动设置方法 |
| 小部件位置丢失 | 小部件位置已重置，请重新拖动到目标位置 | 多显示器切换或分辨率变化时，检测位置有效性 |
| 数据目录创建失败 | 数据目录创建失败，请检查磁盘权限或更换运行位置 | 记录错误，提示用户选择其他目录 |

## 未来扩展方向

AME to do 的初始架构需要允许新增功能模块。以下方向不进入初始版本交付范围，但应在架构上预留可能性。

| 扩展模块 | 说明 |
|---|---|
| 倒班排班表 | 支持白班、夜班、休息、轮班周期记录 |
| 班组协作 | 支持管理层发布班组事项或通知 |
| 工厂通知 | 支持安全提醒、生产通知、培训提醒 |
| 统计看板 | 统计待办完成率、提醒完成情况、事项分布 |
| 数据导入导出 | 支持导入导出 JSON、CSV 或其他格式 |
| 主题与显示模式 | 支持夜间模式、大字体模式 |
| 插件机制 | 支持第三方或内部模块按规范扩展 |

## 需求优先级

| 优先级 | 功能 |
|---|---|
| P0 | Windows 本地运行、基础框架、事项管理、本地数据保存 |
| P0 | 单文件封装、数据目录自动创建、系统托盘 |
| P0 | WebDAV 配置、连接测试、手动同步、同步状态展示 |
| P0 | 万年历公历、农历、节假日、调休查询 |
| P1 | 倒班助手、班次模板、轮班周期、日历班次叠加 |
| P1 | 模块开关管理、模块依赖检测、模块数据保留 |
| P1 | 自动同步、同步冲突处理、节假日实时更新 |
| P1 | 事项重复规则、错过提醒补偿 |
| P1 | 开机启动、启动后最小化、启动后同步 |
| P1 | Windows 桌面小部件、今日概览、待办展示、点击跳转、标准小部件扩展接口 |
| P1 | 平台适配层和 AI Agent 注释规范 |
| P2 | Windows 小部件位置记忆、透明度设置、全屏隐藏、多显示器适配；其他平台同类功能单独设计 |
| P2 | 数据导入导出、更多筛选排序、未来模块扩展接口 |

## 开发约束

1. 初始版本必须保证本地功能不依赖 WebDAV。
2. WebDAV 同步不能静默覆盖用户本地数据。
3. 密码、授权码等敏感信息不能明文写入日志或同步文件。
4. 业务模块不得直接依赖具体平台能力，应通过平台适配层调用。
5. 新增模块必须遵循模块注册机制和数据版本机制。
6. 日历数据、同步数据、用户配置均应带有版本或更新时间。
7. 所有关键逻辑必须加入面向后续维护的注释。
8. 错误提示应让普通用户能理解，同时日志应让开发者能定位问题。
9. 最终交付物应为单文件可执行程序，运行时自动在同级目录创建数据目录。
10. 被禁用的模块不得加载其 UI、不得占用运行时资源，但其本地数据必须保留。
11. 开机启动和桌面小部件应通过平台适配层实现，业务逻辑与 Windows 特定 API 解耦。

## 已确认决策与待确认事项

| 编号 | 事项 | 说明 |
|---|---|---|
| DEC-001 | 技术栈 | 已确认采用 Tauri + TypeScript + 前端跨平台 UI 架构，UI 层需自适应不同 DPI、横竖屏状态和不同长宽比分辨率 |
| DEC-002 | WebDAV 服务兼容范围 | 已确认兼容国内主流 WebDAV 服务和用户自建 WebDAV 服务器 |
| DEC-003 | 节假日数据来源 | 已确认采用中国大陆官方公开数据 |
| DEC-004 | Windows 通知方式 | 已确认使用系统原生通知，NotificationService 做统一跨平台封装 |
| DEC-005 | 数据加密方案 | 已确认采用推荐方案：凭据强保护，业务数据可选加密 |
| DEC-006 | 跨平台框架 | 已确认采用统一跨平台架构 |

## 已确认决策方案

### DEC-001 技术栈（已确认）

采用 `Tauri + TypeScript + 前端跨平台 UI 架构`。Tauri 适合本地优先、体积较小、需要访问文件系统和系统能力的桌面应用；TypeScript 适合维护清晰的数据模型、同步状态和模块接口；前端 UI 层可以复用到后续跨平台版本，降低从 Windows 扩展到 macOS、Linux、Android、iOS、鸿蒙系统时的重写成本。

确认组合如下：

| 层级 | 确认方案 | 原因 |
|---|---|---|
| 桌面应用壳 | Tauri | 适合 Windows 初始版本，应用体积较小，可调用系统文件、通知和安全存储能力 |
| UI 层 | TypeScript + React 或 Vue | 组件化程度高，适合万年历、事项列表、设置页等模块化界面 |
| 状态管理 | 轻量状态管理方案 | 事项、同步状态、配置状态相对清晰，不建议初始版本引入过重框架 |
| 本地存储 | SQLite 或结构化 JSON 文件 | SQLite 更适合后续查询、筛选和迁移；JSON 更简单。推荐优先 SQLite |
| 同步层 | 独立 WebDAV Sync Service | 与 UI 和业务模块解耦，便于后续跨平台复用 |
| 平台能力 | Platform Adapter 平台适配层 | 封装通知、文件路径、安全存储、后台任务等平台差异 |

最终确认：初始版本优先选择 `Tauri + TypeScript + React/Vue + SQLite + WebDAV Sync Service`。如果开发资源较少，可先使用结构化 JSON 文件降低初始实现难度，但需要从第一版开始保留迁移到 SQLite 的数据版本机制。

#### 多平台显示适配要求

UI 层必须从第一版开始支持以下自适应能力，确保后续迁移到不同屏幕尺寸、分辨率和方向的设备时无需大规模重写布局：

| 编号 | 需求 | 说明 |
|---|---|---|
| F-DISP-001 | 自适应 DPI | 界面元素应根据系统 DPI 缩放比例等比缩放，文字和图标不模糊、不溢出。高 DPI 下使用高清资源，低 DPI 下保证可读性 |
| F-DISP-002 | 自适应横竖屏 | 布局应能响应横屏和竖屏切换。竖屏状态下信息区域上下堆叠，横屏状态下可左右分栏。关键信息在两种方向下均可见 |
| F-DISP-003 | 自适应不同长宽比 | 支持从 4:3 到 21:9 等常见长宽比。宽屏下不应出现大面积空白，窄屏下内容不应被截断。日历月视图、事项列表等核心区域应根据可用宽度动态调整列数或卡片尺寸 |
| F-DISP-004 | 最小窗口尺寸 | 应用应定义最小窗口尺寸（建议 360×480），低于该尺寸时停止缩小并显示滚动提示 |
| F-DISP-005 | 字体与触控适配 | 移动端适配预留：字体大小应支持系统字体缩放设置；可点击区域最小尺寸应为 44×44 逻辑像素，确保触控友好 |
| F-DISP-006 | 安全区域适配 | 为移动端刘海屏、挖孔屏和底部导航栏预留安全区域，内容不得被系统 UI 遮挡 |

### DEC-004 Windows 通知方式（已确认）

使用 Windows 系统原生通知能力，并在应用内部封装统一通知接口。业务模块不能直接调用 Windows 通知 API，而应调用 `NotificationService`，由平台适配层负责转换为当前系统支持的通知方式。

统一通知接口包含以下能力：

| 能力 | 说明 |
|---|---|
| showNotification | 展示普通提醒 |
| scheduleNotification | 注册未来提醒，适用于定时事项 |
| cancelNotification | 取消未触发提醒 |
| checkPermission | 检查通知权限或系统可用状态 |
| handleMissedNotifications | 应用启动时处理错过的提醒 |

Windows 初始版本中，到期提醒应优先使用系统通知；如果系统通知不可用，应用应在首页或事项管理模块中展示应用内提醒。后续迁移到 macOS、Android、iOS、Linux、鸿蒙系统时，只替换平台适配层实现，不修改事项管理模块的业务逻辑。

确认结论：使用系统原生通知作为 Windows 初始实现，并以 `NotificationService` 作为跨平台通知抽象。定时事项的业务规则保存在事项管理模块，通知触发和权限差异交给平台适配层处理。

### DEC-005 数据加密方案（已确认）

采用“凭据强保护、业务数据可选加密”的分层策略。WebDAV 密码、授权码等敏感凭据必须使用系统安全存储能力保存，不允许明文写入配置文件、日志文件或 WebDAV 同步文件。事项标题、备注、日历缓存等业务数据默认本地明文保存，以降低初始版本复杂度；后续可提供用户可选的本地数据库加密。

加密策略如下：

| 数据类型 | 处理方式 | 说明 |
|---|---|---|
| WebDAV 密码或授权码 | 系统安全存储 | Windows 使用系统凭据管理能力，其他平台使用对应安全存储 |
| WebDAV 配置地址和用户名 | 本地配置文件保存 | 不属于高敏感凭据，但日志中仍需谨慎脱敏 |
| 事项数据 | 默认本地保存，预留加密开关 | 初始版本优先保证稳定性和可恢复性 |
| 同步文件 | 不包含明文密码或授权码 | 上传到 WebDAV 的文件只能包含业务数据和同步元数据 |
| 日志文件 | 脱敏处理 | 不记录密码、授权码、完整同步地址中的敏感参数 |
| 备份文件 | 跟随业务数据加密策略 | 如果用户开启本地加密，备份文件也应加密 |

确认结论：第一阶段强制加密保存 WebDAV 凭据，业务数据先采用本地结构化存储并预留加密字段和迁移机制。第二阶段再加入“本地数据加密”选项，允许用户为事项数据和备份文件启用加密。
